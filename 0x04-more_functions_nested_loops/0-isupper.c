#include "main.h"
/**
 *  * _isupper - checks upper case
 *   * @c: print nt
 *    * Return: 1 if uppercase and 0 if not
     */

int_isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (0);
}
